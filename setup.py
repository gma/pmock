from distutils.core import setup

setup (
    name = "pmock",
    version = "0.2",
    description = "pmock - a mock objects framework for Python",
    package_dir = {"": "src"},
    py_modules = ["pmock"]
    )
      
      
